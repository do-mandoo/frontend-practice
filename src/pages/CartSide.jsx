import { Box, Button } from '@mui/material';

const CartSide = ({ isOpen, setIsOpen }) => {
  return (
    <Box>
      슬라이드 네브 바에 카트리스트 담을 예정
      <Button>닫기</Button>
    </Box>
  );
};

export default CartSide;
